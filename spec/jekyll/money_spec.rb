require 'spec_helper'

describe Jekyll::Money do
  it 'has a version number' do
    expect(Jekyll::Money::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
